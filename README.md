# make_mkv

Script to mux videos with Hungarian and English subtitles into MKV files.

## Usage

Run the script to process all videos in `to_process/`:

```bash
./process_videos.sh
```

Output files will be created in `outputs/`.

## Requirements

- Conda environment with ffmpeg (e.g., `yt-dl`)
